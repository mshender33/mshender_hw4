<h1>New Place</h1>

<%= form_with model: @place, url: places_path, method: :post do |form| %>
  <p>
    <%= form.label :name, "Place Name" %>
    <%= form.text_field :name, id: "name_input" %>
  </p>

  <%= form.submit "Save", class: "btn btn-primary" %>
<% end %>
